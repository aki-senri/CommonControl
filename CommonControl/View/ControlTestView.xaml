<UserControl
    x:Class="CommonControl.View.ControlTestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CommonControl.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CommonControl.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:CommonControl.ViewModel"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:ControlTestViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style x:Key="ForegroundPaint" TargetType="ComboBox">
            <Style.Triggers>
                <Trigger Property="SelectedIndex" Value="1">
                    <Setter Property="Foreground" Value="Red" />
                </Trigger>
                <Trigger Property="SelectedIndex" Value="0">
                    <Setter Property="Foreground" Value="Gray" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls:CustomComboBox
            Grid.Row="0"
            Width="200"
            Height="30"
            Margin="5"
            BorderBrush="Red"
            BorderThickness="1"
            DefalutIndex="{Binding DefaultIndex}"
            ItemsSource="{Binding TestItems}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            SelectionChanged="CustomComboBox_SelectionChanged" />
        <ComboBox
            Grid.Row="1"
            Width="200"
            Height="30"
            Margin="5"
            ItemsSource="{Binding TestItems}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            SelectionChanged="ComboBox_SelectionChanged"
            Style="{StaticResource ForegroundPaint}" />
    </Grid>
</UserControl>
